# IllumiControl

## Installation

Install the required dependencies using pip:

```bash
  pip install -r requirements.txt 
``` 

## Usage

Run the main script to start the application with the specified configuration file:

```bash
  python main.py  --config=/path/to/run-config.ini
```
